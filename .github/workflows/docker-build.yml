name: Build and Push Docker Images

on:
  push:
    branches: [ main ]

env:
  LIBREOFFICE_VERSION: "26.2.1"

jobs:
  build-base:
    name: Build LibreOffice Base Image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push base image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: runtime/Dockerfile
          push: true
          build-args: |
            LIBREOFFICE_VERSION=${{ env.LIBREOFFICE_VERSION }}
          tags: ${{ secrets.DOCKER_USERNAME }}/libreoffice:${{ env.LIBREOFFICE_VERSION }}

  build-app:
    name: Build App Image
    runs-on: ubuntu-latest
    needs: build-base
    steps:
      - uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push app image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          push: true
          build-args: |
            BASE_VERSION=${{ env.LIBREOFFICE_VERSION }}
            DOCKER_USERNAME=${{ secrets.DOCKER_USERNAME }}
          tags: ${{ secrets.DOCKER_USERNAME }}/jodconverter-rest:latest
